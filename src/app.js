import React from 'react';
import ReactDOM from 'react-dom';

const App = () => {
    return(
      <div>This is going away</div>
    );
}

ReactDOM.render(<App />, document.getElementById('nf-root'));
